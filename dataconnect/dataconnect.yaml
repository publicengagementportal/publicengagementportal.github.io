specVersion: "v1beta"
serviceId: "pep-redone"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "pep-redone-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
